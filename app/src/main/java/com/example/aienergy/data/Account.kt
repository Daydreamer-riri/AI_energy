package com.example.aienergy.data

import android.icu.math.BigDecimal

class Account(
    val accountNumber: Number,
    val balance: BigDecimal,
) {
    init {
        val accountNumber: Number = accountNumber
        val balance: Number = balance
    }
}